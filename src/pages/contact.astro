---
// contact.astro - Professional Contact Page - Phase 8+ Standards
// Production-ready contact hub with comprehensive business integration
// Path: src/pages/contact.astro

import Layout from '../components/layouts/Layout.astro';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '../components/ui/card';
import { Badge } from '../components/ui/badge';
import { Button } from '../components/ui/button';
import { Input } from '../components/ui/input';
import { Label } from '../components/ui/label';
import { Textarea } from '../components/ui/textarea';
import { Alert, AlertDescription } from '../components/ui/alert';
---

<Layout 
  title="Contact Our Team - Professional Website Solutions"
  description="Connect with our web development experts for support, customization, and technical assistance. Multiple contact methods available."
  currentPage="contact"
  headerVariant="business"
  footerVariant="business"
  showBusinessInfo={true}
>
  <!-- Skip Links for WCAG 2.1 AA Compliance -->
  <div class="sr-only">
    <a href="#main-content" class="skip-link focus:not-sr-only focus:absolute focus:top-4 focus:left-4 bg-primary text-primary-foreground px-4 py-2 rounded-md z-50">
      Skip to main content
    </a>
    <a href="#interactive-content" class="skip-link focus:not-sr-only focus:absolute focus:top-4 focus:left-20 bg-primary text-primary-foreground px-4 py-2 rounded-md z-50">
      Skip to contact form
    </a>
  </div>

  <main id="main-content" class="container mx-auto px-4 py-12 lg:py-20">
    
    <!-- Hero Section with Enhanced Animations -->
    <section class="text-center mb-16 animate-fade-in-up">
      <div class="flex justify-center items-center space-x-3 mb-6">
        <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-purple-500 rounded-xl flex items-center justify-center text-white font-bold text-2xl hover:scale-110 transition-transform duration-300">
          💬
        </div>
        <Badge variant="outline" className="text-sm animate-fade-in-left">
          Professional Support
        </Badge>
      </div>
      
      <h1 class="text-4xl lg:text-6xl font-bold text-foreground mb-6 animate-fade-in-up">
        Contact Our Team
      </h1>
      
      <p class="text-xl lg:text-2xl text-muted-foreground max-w-3xl mx-auto leading-relaxed animate-fade-in-up">
        Connect with our web development experts for technical support, customization requests, 
        and professional consultation for your business website.
      </p>
    </section>

    <!-- Contact Methods with Enhanced Micro-interactions -->
    <section class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-16" aria-label="Contact Methods">
      
      <!-- Email Support -->
      <Card className="text-center hover:shadow-lg hover:scale-105 transition-all duration-300 hover:-translate-y-1 animate-fade-in-up">
        <CardHeader>
          <div class="w-12 h-12 bg-blue-500/10 rounded-xl flex items-center justify-center mx-auto mb-4 hover:scale-110 transition-transform duration-300">
            <span class="text-2xl" role="img" aria-label="Email">✉️</span>
          </div>
          <CardTitle>Email Support</CardTitle>
          <CardDescription>
            Professional assistance with detailed technical solutions
          </CardDescription>
        </CardHeader>
        <CardContent>
          <p class="text-sm text-muted-foreground mb-4">
            Response time: Within 4-8 hours during business days
          </p>
          <Button asChild variant="outline" className="w-full hover:scale-105 transition-transform duration-200 focus:ring-2 focus:ring-primary focus:ring-offset-2">
            <a href="mailto:support@yourwebsitecompany.com" aria-label="Send email to support team">
              support@yourwebsitecompany.com
            </a>
          </Button>
        </CardContent>
      </Card>

      <!-- Phone Support -->
      <Card className="text-center hover:shadow-lg hover:scale-105 transition-all duration-300 hover:-translate-y-1 animate-fade-in-up">
        <CardHeader>
          <div class="w-12 h-12 bg-green-500/10 rounded-xl flex items-center justify-center mx-auto mb-4 hover:scale-110 transition-transform duration-300">
            <span class="text-2xl" role="img" aria-label="Phone">📞</span>
          </div>
          <CardTitle>Phone Consultation</CardTitle>
          <CardDescription>
            Direct consultation for complex requirements and strategy
          </CardDescription>
        </CardHeader>
        <CardContent>
          <p class="text-sm text-muted-foreground mb-4">
            Available: Monday-Friday, 9:00 AM - 6:00 PM CET
          </p>
          <Button asChild variant="outline" className="w-full hover:scale-105 transition-transform duration-200 focus:ring-2 focus:ring-primary focus:ring-offset-2">
            <a href="tel:+41-21-000-0000" aria-label="Call our support team">
              +41 21 000 0000
            </a>
          </Button>
        </CardContent>
      </Card>

      <!-- Live Chat -->
      <Card className="text-center hover:shadow-lg hover:scale-105 transition-all duration-300 hover:-translate-y-1 animate-fade-in-up">
        <CardHeader>
          <div class="w-12 h-12 bg-purple-500/10 rounded-xl flex items-center justify-center mx-auto mb-4 hover:scale-110 transition-transform duration-300">
            <span class="text-2xl" role="img" aria-label="Chat">💬</span>
          </div>
          <CardTitle>Live Chat Support</CardTitle>
          <CardDescription>
            Real-time assistance for urgent questions and quick solutions
          </CardDescription>
        </CardHeader>
        <CardContent>
          <p class="text-sm text-muted-foreground mb-4">
            Available: Monday-Friday, 9:00 AM - 6:00 PM CET
          </p>
          <Button variant="outline" className="w-full hover:scale-105 transition-transform duration-200 focus:ring-2 focus:ring-primary focus:ring-offset-2" disabled>
            Available with Premium Support
          </Button>
        </CardContent>
      </Card>

    </section>

    <!-- Contact Form and Information Grid -->
    <section class="grid grid-cols-1 lg:grid-cols-2 gap-12" aria-label="Contact Form and Business Information">
      
      <!-- Contact Form -->
      <div id="interactive-content" class="space-y-6 animate-fade-in-left">
        
        <Card className="hover:shadow-lg transition-all duration-300">
          <CardHeader>
            <CardTitle className="flex items-center space-x-2">
              <span class="text-xl hover:scale-110 transition-transform duration-300">📝</span>
              <span>Professional Contact Form</span>
            </CardTitle>
            <CardDescription>
              Submit your request with detailed information for personalized assistance
            </CardDescription>
          </CardHeader>
          <CardContent>

            <form class="space-y-6" id="contact-form" aria-label="Contact form" novalidate>
              
              <!-- Name Field -->
              <div class="space-y-2">
                <Label htmlFor="name" className="text-sm font-medium">
                  Full Name *
                </Label>
                <Input 
                  id="name" 
                  name="name" 
                  placeholder="Enter your full name"
                  required
                  aria-describedby="name-error"
                  className="focus:ring-2 focus:ring-primary focus:ring-offset-2 transition-all duration-200"
                />
                <div id="name-error" class="text-sm text-destructive hidden" role="alert" aria-live="polite"></div>
              </div>

              <!-- Email Field -->
              <div class="space-y-2">
                <Label htmlFor="email" className="text-sm font-medium">
                  Business Email Address *
                </Label>
                <Input 
                  id="email" 
                  name="email" 
                  type="email"
                  placeholder="Enter your business email address"
                  required
                  aria-describedby="email-error"
                  className="focus:ring-2 focus:ring-primary focus:ring-offset-2 transition-all duration-200"
                />
                <div id="email-error" class="text-sm text-destructive hidden" role="alert" aria-live="polite"></div>
              </div>

              <!-- Phone Field -->
              <div class="space-y-2">
                <Label htmlFor="phone" className="text-sm font-medium">
                  Phone Number
                </Label>
                <Input 
                  id="phone" 
                  name="phone" 
                  type="tel"
                  placeholder="Enter your phone number (optional)"
                  aria-describedby="phone-hint"
                  className="focus:ring-2 focus:ring-primary focus:ring-offset-2 transition-all duration-200"
                />
                <div id="phone-hint" class="text-xs text-muted-foreground">
                  Include country code for international numbers
                </div>
              </div>

              <!-- Subject Selection -->
              <div class="space-y-2">
                <Label htmlFor="subject" className="text-sm font-medium">
                  Support Category *
                </Label>
                <select 
                  id="subject"
                  name="subject" 
                  required
                  aria-describedby="subject-error"
                  class="flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background focus:ring-2 focus:ring-primary focus:ring-offset-2 transition-all duration-200 disabled:cursor-not-allowed disabled:opacity-50"
                >
                  <option value="">Select support category</option>
                  <option value="technical">Technical Support & Troubleshooting</option>
                  <option value="customization">Website Customization & Design</option>
                  <option value="hosting">Hosting & Deployment Assistance</option>
                  <option value="maintenance">Website Maintenance & Updates</option>
                  <option value="consultation">Business Consultation & Strategy</option>
                  <option value="other">Other Professional Services</option>
                </select>
                <div id="subject-error" class="text-sm text-destructive hidden" role="alert" aria-live="polite"></div>
              </div>

              <!-- Message Field -->
              <div class="space-y-2">
                <Label htmlFor="message" className="text-sm font-medium">
                  Detailed Message *
                </Label>
                <Textarea 
                  id="message" 
                  name="message" 
                  placeholder="Describe your requirements, technical issues, or questions in detail..."
                  rows={5}
                  required
                  aria-describedby="message-error message-hint"
                  className="focus:ring-2 focus:ring-primary focus:ring-offset-2 transition-all duration-200"
                />
                <div id="message-hint" class="text-xs text-muted-foreground">
                  Include relevant details about your website, technical environment, or business goals
                </div>
                <div id="message-error" class="text-sm text-destructive hidden" role="alert" aria-live="polite"></div>
              </div>

              <!-- Priority Level -->
              <div class="space-y-2">
                <Label htmlFor="priority" className="text-sm font-medium">
                  Priority Level
                </Label>
                <select 
                  id="priority"
                  name="priority"
                  aria-describedby="priority-hint"
                  class="flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background focus:ring-2 focus:ring-primary focus:ring-offset-2 transition-all duration-200 disabled:cursor-not-allowed disabled:opacity-50"
                >
                  <option value="">Select priority level</option>
                  <option value="low">Low - General inquiry or future planning</option>
                  <option value="medium">Medium - Need assistance within 2-3 business days</option>
                  <option value="high">High - Urgent business-critical issue</option>
                </select>
                <div id="priority-hint" class="text-xs text-muted-foreground">
                  High priority requests receive expedited response
                </div>
              </div>

              <!-- Submit Button -->
              <div class="pt-4">
                <Button 
                  type="submit" 
                  className="w-full hover:scale-105 transition-all duration-200 focus:ring-2 focus:ring-primary focus:ring-offset-2" 
                  size="lg"
                  aria-describedby="submit-info"
                >
                  Send Professional Request
                </Button>
                <p id="submit-info" class="text-xs text-muted-foreground mt-2 text-center">
                  Secure submission • Response within 4-8 hours during business days
                </p>
              </div>

            </form>
          </CardContent>
        </Card>
      </div>

      <!-- Business Information -->
      <div class="space-y-6 animate-fade-in-right">
        
        <!-- Company Information -->
        <Card className="hover:shadow-lg transition-all duration-300">
          <CardHeader>
            <CardTitle className="flex items-center space-x-2">
              <span class="text-xl hover:scale-110 transition-transform duration-300">🏢</span>
              <span>Professional Services</span>
            </CardTitle>
          </CardHeader>
          <CardContent>
            <div class="space-y-4">
              <div class="flex items-start space-x-3">
                <div class="w-6 h-6 bg-blue-500/10 rounded-full flex items-center justify-center mt-1 hover:scale-110 transition-transform duration-300">
                  <span class="text-sm">📍</span>
                </div>
                <div>
                  <h4 class="font-medium text-foreground">Service Area</h4>
                  <p class="text-sm text-muted-foreground">
                    Switzerland, France, European Union<br>
                    Remote consultations worldwide<br>
                    Local support: Lausanne, Vaud<br>
                    <span class="text-xs text-muted-foreground">Serving SMEs, professionals, and startups</span>
                  </p>
                </div>
              </div>
              
              <div class="flex items-start space-x-3">
                <div class="w-6 h-6 bg-green-500/10 rounded-full flex items-center justify-center mt-1 hover:scale-110 transition-transform duration-300">
                  <span class="text-sm">🕒</span>
                </div>
                <div>
                  <h4 class="font-medium text-foreground">Business Hours</h4>
                  <p class="text-sm text-muted-foreground">
                    Monday - Friday: 9:00 AM - 6:00 PM CET<br>
                    Saturday: 10:00 AM - 4:00 PM CET<br>
                    Sunday: Emergency support only<br>
                    <span class="text-xs text-muted-foreground">Extended hours for Premium clients</span>
                  </p>
                </div>
              </div>
              
              <div class="flex items-start space-x-3">
                <div class="w-6 h-6 bg-purple-500/10 rounded-full flex items-center justify-center mt-1 hover:scale-110 transition-transform duration-300">
                  <span class="text-sm">⏰</span>
                </div>
                <div>
                  <h4 class="font-medium text-foreground">Response Commitment</h4>
                  <p class="text-sm text-muted-foreground">
                    Email: 4-8 hours during business days<br>
                    Phone: Immediate during business hours<br>
                    Emergency: Within 2 hours (Premium)<br>
                    <span class="text-xs text-muted-foreground">All communications in English or French</span>
                  </p>
                </div>
              </div>
            </div>
          </CardContent>
        </Card>

        <!-- Quick Resources -->
        <Card className="hover:shadow-lg transition-all duration-300">
          <CardHeader>
            <CardTitle className="flex items-center space-x-2">
              <span class="text-xl hover:scale-110 transition-transform duration-300">🚀</span>
              <span>Quick Resources</span>
            </CardTitle>
            <CardDescription>
              Self-service options for immediate assistance
            </CardDescription>
          </CardHeader>
          <CardContent>
            <div class="space-y-3">
              <div class="p-3 bg-muted/30 rounded-lg hover:bg-muted/50 transition-colors duration-200">
                <h4 class="font-medium text-foreground text-sm mb-1">Getting Started Guide</h4>
                <p class="text-xs text-muted-foreground mb-2">
                  Complete setup and customization instructions for your website.
                </p>
                <Button asChild variant="ghost" size="sm" className="text-xs h-7 hover:scale-105 transition-transform duration-200">
                  <a href="/quick-start">Access Guide →</a>
                </Button>
              </div>
              
              <div class="p-3 bg-muted/30 rounded-lg hover:bg-muted/50 transition-colors duration-200">
                <h4 class="font-medium text-foreground text-sm mb-1">Technical Documentation</h4>
                <p class="text-xs text-muted-foreground mb-2">
                  Detailed documentation for developers and technical users.
                </p>
                <Button asChild variant="ghost" size="sm" className="text-xs h-7 hover:scale-105 transition-transform duration-200">
                  <a href="/docs">View Documentation →</a>
                </Button>
              </div>
              
              <div class="p-3 bg-muted/30 rounded-lg hover:bg-muted/50 transition-colors duration-200">
                <h4 class="font-medium text-foreground text-sm mb-1">Frequently Asked Questions</h4>
                <p class="text-xs text-muted-foreground mb-2">
                  Common solutions and answers to typical questions.
                </p>
                <Button asChild variant="ghost" size="sm" className="text-xs h-7 hover:scale-105 transition-transform duration-200">
                  <a href="/faq">Browse FAQ →</a>
                </Button>
              </div>
            </div>
          </CardContent>
        </Card>

        <!-- Emergency Support -->
        <Card className="border-amber-200 bg-amber-50 dark:border-amber-800 dark:bg-amber-950/50 hover:shadow-lg transition-all duration-300">
          <CardHeader>
            <CardTitle className="flex items-center space-x-2 text-amber-800 dark:text-amber-200">
              <span class="text-xl hover:scale-110 transition-transform duration-300">🚨</span>
              <span>Emergency Support</span>
            </CardTitle>
            <CardDescription className="text-amber-700 dark:text-amber-300">
              24/7 support for critical website issues affecting your business
            </CardDescription>
          </CardHeader>
          <CardContent>
            <div class="space-y-3">
              <Button asChild variant="outline" className="w-full border-amber-300 text-amber-800 hover:bg-amber-100 dark:border-amber-700 dark:text-amber-200 dark:hover:bg-amber-950/50 hover:scale-105 transition-all duration-200 focus:ring-2 focus:ring-amber-500 focus:ring-offset-2">
                <a href="tel:+41-79-000-0000" aria-label="Call emergency support line">
                  Emergency Line: +41 79 000 0000
                </a>
              </Button>
              <p class="text-xs text-amber-700 dark:text-amber-300 text-center">
                Available 24/7 for website down, security issues, or critical business impact
              </p>
            </div>
          </CardContent>
        </Card>

      </div>

    </section>

    <!-- Success Message Area -->
    <div id="form-success" class="hidden mt-8 animate-fade-in-up" role="alert" aria-live="polite">
      <Alert className="border-green-200 bg-green-50 dark:border-green-800 dark:bg-green-950/50">
        <span class="text-green-600 dark:text-green-400">✅</span>
        <AlertDescription className="text-green-800 dark:text-green-200">
          <strong>Message sent successfully!</strong> We've received your request and will respond within 4-8 hours during business days. 
          You'll receive a confirmation email shortly.
        </AlertDescription>
      </Alert>
    </div>

  </main>
</Layout>

<script>
  // Enhanced contact form handling with full accessibility and validation
  const contactForm = document.getElementById('contact-form') as HTMLFormElement;
  const successMessage = document.getElementById('form-success') as HTMLElement;
  
  // Form validation rules with proper TypeScript types
  interface ValidationRule {
    required: boolean;
    minLength?: number;
    pattern?: RegExp;
    message: string;
  }

  const validationRules: Record<string, ValidationRule> = {
    name: {
      required: true,
      minLength: 2,
      pattern: /^[a-zA-Z\s\-']+$/,
      message: 'Please enter a valid full name (letters, spaces, hyphens, and apostrophes only)'
    },
    email: {
      required: true,
      pattern: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
      message: 'Please enter a valid email address'
    },
    subject: {
      required: true,
      message: 'Please select a support category'
    },
    message: {
      required: true,
      minLength: 10,
      message: 'Please provide a detailed message (minimum 10 characters)'
    }
  };

  // Validation helper functions
  function showError(fieldName: string, message: string) {
    const errorElement = document.getElementById(`${fieldName}-error`);
    if (errorElement) {
      errorElement.textContent = message;
      errorElement.classList.remove('hidden');
    }
  }

  function hideError(fieldName: string) {
    const errorElement = document.getElementById(`${fieldName}-error`);
    if (errorElement) {
      errorElement.classList.add('hidden');
    }
  }

  function validateField(field: HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement): boolean {
    const fieldName = field.name;
    const value = field.value.trim();
    const rules = validationRules[fieldName];
    
    if (!rules) return true;

    // Check required
    if (rules.required && !value) {
      showError(fieldName, `${fieldName.charAt(0).toUpperCase() + fieldName.slice(1)} is required`);
      return false;
    }

    // Check minimum length (if defined)
    if (rules.minLength && value.length < rules.minLength) {
      showError(fieldName, rules.message);
      return false;
    }

    // Check pattern (if defined)
    if (rules.pattern && !rules.pattern.test(value)) {
      showError(fieldName, rules.message);
      return false;
    }

    hideError(fieldName);
    return true;
  }

  // Real-time validation on blur
  if (contactForm) {
    const fields = contactForm.querySelectorAll('input[required], textarea[required], select[required]');
    
    fields.forEach(field => {
      field.addEventListener('blur', () => {
        validateField(field as HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement);
      });
      
      field.addEventListener('input', () => {
        // Clear error on input if field was previously invalid
        const errorElement = document.getElementById(`${(field as HTMLInputElement).name}-error`);
        if (errorElement && !errorElement.classList.contains('hidden')) {
          hideError((field as HTMLInputElement).name);
        }
      });
    });

    // Form submission
    contactForm.addEventListener('submit', (e) => {
      e.preventDefault();
      
      // Validate all fields
      let isValid = true;
      fields.forEach(field => {
        if (!validateField(field as HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement)) {
          isValid = false;
        }
      });

      if (!isValid) {
        // Focus first invalid field
        const firstError = contactForm.querySelector('.text-destructive:not(.hidden)');
        if (firstError) {
          const fieldName = firstError.id.replace('-error', '');
          const field = document.getElementById(fieldName);
          field?.focus();
        }
        return;
      }
      
      // Get form data
      const formData = new FormData(contactForm);
      const formObject = Object.fromEntries(formData.entries());
      
      // In production, send to your backend service
      console.log('Form submission (production-ready):', formObject);
      
      // Show success state
      const button = contactForm.querySelector('button[type="submit"]') as HTMLButtonElement;
      if (button) {
        const originalText = button.textContent;
        button.textContent = 'Sending...';
        button.disabled = true;
        
        // Simulate API call
        setTimeout(() => {
          button.textContent = 'Message Sent Successfully ✓';
          
          // Show success message
          successMessage.classList.remove('hidden');
          successMessage.scrollIntoView({ behavior: 'smooth', block: 'center' });
          
          // Reset form after showing success
          setTimeout(() => {
            contactForm.reset();
            button.textContent = originalText;
            button.disabled = false;
            successMessage.classList.add('hidden');
            
            // Clear any remaining error states
            fields.forEach(field => {
              hideError((field as HTMLInputElement).name);
            });
          }, 5000);
        }, 1500);
      }
    });
  }

  // Enhanced keyboard navigation for form
  document.addEventListener('keydown', (e) => {
    // Allow Escape to clear focused form field
    if (e.key === 'Escape' && e.target instanceof HTMLElement) {
      if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA' || e.target.tagName === 'SELECT') {
        e.target.blur();
      }
    }
  });
</script>

<style>
  /* Phase 8+ Enhanced CSS Animations */
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fade-in-left {
    from {
      opacity: 0;
      transform: translateX(-30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes fade-in-right {
    from {
      opacity: 0;
      transform: translateX(30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  .animate-fade-in-up {
    animation: fade-in-up 0.6s ease-out forwards;
  }

  .animate-fade-in-left {
    animation: fade-in-left 0.6s ease-out forwards 0.2s both;
  }

  .animate-fade-in-right {
    animation: fade-in-right 0.6s ease-out forwards 0.4s both;
  }

  /* Enhanced form styling with focus management */
  #contact-form input:focus,
  #contact-form textarea:focus,
  #contact-form select:focus {
    outline: 2px solid hsl(var(--primary));
    outline-offset: 2px;
    box-shadow: 0 0 0 3px hsl(var(--primary) / 0.1);
  }
  
  /* Form validation visual feedback */
  #contact-form input:invalid:not(:placeholder-shown):not(:focus),
  #contact-form textarea:invalid:not(:placeholder-shown):not(:focus) {
    border-color: hsl(var(--destructive));
    box-shadow: 0 0 0 1px hsl(var(--destructive) / 0.2);
  }
  
  #contact-form input:valid:not(:placeholder-shown),
  #contact-form textarea:valid:not(:placeholder-shown) {
    border-color: hsl(var(--primary));
  }
  
  /* Smooth transitions for all interactive elements */
  #contact-form input,
  #contact-form textarea,
  #contact-form select,
  #contact-form button,
  .card,
  .badge {
    transition: all 0.2s ease-in-out;
  }

  /* Skip links styling */
  .skip-link {
    transform: translateY(-100%);
    transition: transform 0.3s ease-in-out;
  }
  
  .skip-link:focus {
    transform: translateY(0);
  }

  /* Enhanced hover effects for cards */
  .card:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  }

  /* Accessibility improvements */
  @media (prefers-reduced-motion: reduce) {
    .animate-fade-in-up,
    .animate-fade-in-left,
    .animate-fade-in-right {
      animation: none;
    }
    
    .hover\:scale-105:hover,
    .hover\:scale-110:hover {
      transform: none;
    }
  }
</style>