---
// Scroll Indicators Showcase Page - Sprint 9 Step 4 Demo
// Complete demonstration of all scroll tracking and navigation components
// Path: src/pages/scroll-indicators-showcase.astro

import Layout from '../components/layouts/Layout.astro';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '../components/ui/card';
import { Badge } from '../components/ui/badge';
import { Button } from '../components/ui/button';
import { 
  ReadingProgress, 
  ScrollProgressRing, 
  TableOfContents, 
  SectionNavigator,
  PageProgressBar,
  ScrollToTop
} from '../components/islands/ScrollIndicators';
---

<Layout 
  title="Scroll Indicators Showcase - Sprint 9"
  description="Complete demonstration of scroll tracking, reading progress, and navigation indicators"
  currentPage="scroll-showcase"
  headerVariant="business"
  footerVariant="business"
  keywords={["scroll indicators, reading progress, table of contents, scroll tracking, navigation"]}
>
  <!-- Reading Progress at Top -->
  <ReadingProgress client:load position="top" height={4} showPercentage={false} aria-label="Reading progress indicator showing page completion percentage" />
  
  <!-- Page Progress Bar at Bottom -->
  <PageProgressBar client:load position="bottom" thickness="medium" style="gradient" aria-label="Enhanced page progress bar with gradient styling" />

  <!-- Hero Section -->
  <section id="introduction" class="bg-gradient-to-br from-primary/5 via-background to-secondary/5 py-16" role="banner" aria-labelledby="hero-title">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center">
        <div class="flex items-center justify-center space-x-2 mb-6">
          <span class="text-4xl" aria-hidden="true">📊</span>
          <Badge variant="secondary" className="text-sm font-medium">
            Sprint 9 - Step 4
          </Badge>
        </div>
        
        <h1 id="hero-title" class="text-4xl lg:text-5xl font-bold text-foreground mb-6">
          Scroll Indicators Showcase
        </h1>
        
        <p class="text-xl text-muted-foreground mb-8 leading-relaxed">
          Complete scroll tracking system with reading progress, table of contents, 
          and modern navigation indicators for professional web applications.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center" role="group" aria-label="Scroll indicators exploration actions">
          <Button size="lg" className="text-lg px-8" aria-label="Learn about reading progress indicators">
            <span class="mr-2" aria-hidden="true">📖</span>
            Reading Progress
          </Button>
          <Button variant="outline" size="lg" className="text-lg px-8" aria-label="Explore navigation tracking components">
            <span class="mr-2" aria-hidden="true">🧭</span>
            Navigation Tracking
          </Button>
        </div>
      </div>
    </div>
  </section>

  <!-- Active Indicators Info -->
  <section id="overview" class="py-8 bg-muted/30" role="region" aria-labelledby="active-indicators">
    <div class="container mx-auto px-4">
      <Card className="border-primary/20 bg-primary/5">
        <CardHeader>
          <CardTitle id="active-indicators" className="flex items-center space-x-2">
            <span aria-hidden="true">📊</span>
            <span>Active Scroll Indicators on This Page</span>
          </CardTitle>
          <CardDescription>
            Multiple scroll indicators are currently active. Try scrolling to see them in action!
          </CardDescription>
        </CardHeader>
        <CardContent>
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 text-sm" role="list" aria-label="Currently active scroll indicator components">
            <div role="listitem">
              <strong>Reading Progress:</strong> Top bar showing % completion
            </div>
            <div role="listitem">
              <strong>Progress Ring:</strong> Circular indicator (bottom-right)
            </div>
            <div role="listitem">
              <strong>Table of Contents:</strong> Fixed navigation (right side)
            </div>
            <div role="listitem">
              <strong>Section Navigator:</strong> Dot navigation (left side)
            </div>
            <div role="listitem">
              <strong>Page Progress Bar:</strong> Gradient bar (bottom)
            </div>
            <div role="listitem">
              <strong>Scroll to Top:</strong> Appears after scrolling down
            </div>
          </div>
        </CardContent>
      </Card>
    </div>
  </section>

  <!-- Components Overview -->
  <section id="components" class="py-16" role="region" aria-labelledby="components-title">
    <div class="container mx-auto px-4">
      <div class="text-center mb-12">
        <h2 id="components-title" class="text-3xl font-bold text-foreground mb-4">
          Scroll Indicator Components
        </h2>
        <p class="text-lg text-muted-foreground max-w-3xl mx-auto">
          Professional scroll tracking components designed for modern web applications
          with business-ready configurations and optimal user experience.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6" role="list" aria-label="Available scroll indicator component types">
        
        <!-- Reading Progress -->
        <Card className="hover:shadow-lg transition-all duration-200 hover:scale-105" role="listitem">
          <CardHeader className="text-center">
            <div class="text-3xl mb-2" aria-hidden="true">📊</div>
            <CardTitle className="text-lg">Reading Progress</CardTitle>
            <CardDescription>
              Linear progress bar for reading tracking
            </CardDescription>
          </CardHeader>
          <CardContent>
            <ul class="text-sm space-y-2 text-muted-foreground" role="list" aria-label="Reading progress component features">
              <li role="listitem">• <strong>Position:</strong> Top or bottom of page</li>
              <li role="listitem">• <strong>Features:</strong> Percentage display, custom colors</li>
              <li role="listitem">• <strong>Use case:</strong> Articles, blogs, documentation</li>
              <li role="listitem">• <strong>Performance:</strong> GPU-accelerated smooth animation</li>
            </ul>
          </CardContent>
        </Card>

        <!-- Scroll Progress Ring -->
        <Card className="hover:shadow-lg transition-all duration-200 hover:scale-105" role="listitem">
          <CardHeader className="text-center">
            <div class="text-3xl mb-2" aria-hidden="true">⭕</div>
            <CardTitle className="text-lg">Progress Ring</CardTitle>
            <CardDescription>
              Circular progress indicator with percentage
            </CardDescription>
          </CardHeader>
          <CardContent>
            <ul class="text-sm space-y-2 text-muted-foreground" role="list" aria-label="Progress ring component features">
              <li role="listitem">• <strong>Style:</strong> Modern circular design</li>
              <li role="listitem">• <strong>Position:</strong> Fixed corners or custom</li>
              <li role="listitem">• <strong>Customization:</strong> Size, colors, stroke width</li>
              <li role="listitem">• <strong>Best for:</strong> Minimal, elegant interfaces</li>
            </ul>
          </CardContent>
        </Card>

        <!-- Table of Contents -->
        <Card className="hover:shadow-lg transition-all duration-200 hover:scale-105" role="listitem">
          <CardHeader className="text-center">
            <div class="text-3xl mb-2" aria-hidden="true">📋</div>
            <CardTitle className="text-lg">Table of Contents</CardTitle>
            <CardDescription>
              Smart navigation with scroll tracking
            </CardDescription>
          </CardHeader>
          <CardContent>
            <ul class="text-sm space-y-2 text-muted-foreground" role="list" aria-label="Table of contents component features">
              <li role="listitem">• <strong>Auto-tracking:</strong> Highlights current section</li>
              <li role="listitem">• <strong>Nested support:</strong> Multi-level headings</li>
              <li role="listitem">• <strong>Smooth scroll:</strong> Click to navigate</li>
              <li role="listitem">• <strong>Responsive:</strong> Adapts to content structure</li>
            </ul>
          </CardContent>
        </Card>

        <!-- Section Navigator -->
        <Card className="hover:shadow-lg transition-all duration-200 hover:scale-105" role="listitem">
          <CardHeader className="text-center">
            <div class="text-3xl mb-2" aria-hidden="true">🔘</div>
            <CardTitle className="text-lg">Section Navigator</CardTitle>
            <CardDescription>
              Minimal dot-style section navigation
            </CardDescription>
          </CardHeader>
          <CardContent>
            <ul class="text-sm space-y-2 text-muted-foreground" role="list" aria-label="Section navigator component features">
              <li role="listitem">• <strong>Style:</strong> Clean dot indicators</li>
              <li role="listitem">• <strong>Interaction:</strong> Hover labels, smooth scroll</li>
              <li role="listitem">• <strong>Position:</strong> Left or right fixed</li>
              <li role="listitem">• <strong>Perfect for:</strong> Landing pages, portfolios</li>
            </ul>
          </CardContent>
        </Card>

        <!-- Page Progress Bar -->
        <Card className="hover:shadow-lg transition-all duration-200 hover:scale-105" role="listitem">
          <CardHeader className="text-center">
            <div class="text-3xl mb-2" aria-hidden="true">📏</div>
            <CardTitle className="text-lg">Page Progress Bar</CardTitle>
            <CardDescription>
              Enhanced progress bar with styles
            </CardDescription>
          </CardHeader>
          <CardContent>
            <ul class="text-sm space-y-2 text-muted-foreground" role="list" aria-label="Page progress bar component features">
              <li role="listitem">• <strong>Styles:</strong> Solid, gradient, animated</li>
              <li role="listitem">• <strong>Thickness:</strong> Thin, medium, thick options</li>
              <li role="listitem">• <strong>Position:</strong> Top or bottom placement</li>
              <li role="listitem">• <strong>Use case:</strong> Enhanced reading experience</li>
            </ul>
          </CardContent>
        </Card>

        <!-- Scroll to Top -->
        <Card className="hover:shadow-lg transition-all duration-200 hover:scale-105" role="listitem">
          <CardHeader className="text-center">
            <div class="text-3xl mb-2" aria-hidden="true">⬆️</div>
            <CardTitle className="text-lg">Scroll to Top</CardTitle>
            <CardDescription>
              Smart back-to-top with progress option
            </CardDescription>
          </CardHeader>
          <CardContent>
            <ul class="text-sm space-y-2 text-muted-foreground" role="list" aria-label="Scroll to top component features">
              <li role="listitem">• <strong>Smart show:</strong> Appears after scroll threshold</li>
              <li role="listitem">• <strong>Variants:</strong> Button, FAB, minimal styles</li>
              <li role="listitem">• <strong>Progress:</strong> Optional percentage display</li>
              <li role="listitem">• <strong>Smooth animation:</strong> Fade in/out transitions</li>
            </ul>
          </CardContent>
        </Card>

      </div>
    </div>
  </section>

  <!-- Implementation Examples -->
  <section id="implementation" class="py-16 bg-muted/30" role="region" aria-labelledby="implementation-title">
    <div class="container mx-auto px-4">
      <h2 id="implementation-title" class="text-3xl font-bold text-foreground text-center mb-12">
        Implementation Examples
      </h2>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8" role="list" aria-label="Code implementation examples">
        
        <!-- Basic Reading Progress -->
        <Card role="listitem">
          <CardHeader>
            <CardTitle className="flex items-center space-x-2">
              <span aria-hidden="true">📊</span>
              <span>Basic Reading Progress</span>
            </CardTitle>
            <CardDescription>
              Simple progress bar at page top
            </CardDescription>
          </CardHeader>
          <CardContent>
            <div class="bg-muted p-4 rounded-lg text-sm font-mono" role="code" aria-label="Basic reading progress implementation code">
              {`<ReadingProgress 
  position="top"
  height={3}
  color="primary"
  showPercentage={false}
/>`}
            </div>
          </CardContent>
        </Card>

        <!-- Progress Ring -->
        <Card role="listitem">
          <CardHeader>
            <CardTitle className="flex items-center space-x-2">
              <span aria-hidden="true">⭕</span>
              <span>Circular Progress Ring</span>
            </CardTitle>
            <CardDescription>
              Modern circular indicator with percentage
            </CardDescription>
          </CardHeader>
          <CardContent>
            <div class="bg-muted p-4 rounded-lg text-sm font-mono" role="code" aria-label="Circular progress ring implementation code">
              {`<ScrollProgressRing
  size={60}
  position="bottom-right"
  showPercentage={true}
  color="hsl(var(--primary))"
/>`}
            </div>
          </CardContent>
        </Card>

        <!-- Table of Contents -->
        <Card role="listitem">
          <CardHeader>
            <CardTitle className="flex items-center space-x-2">
              <span aria-hidden="true">📋</span>
              <span>Auto-Generated TOC</span>
            </CardTitle>
            <CardDescription>
              Smart table of contents from headings
            </CardDescription>
          </CardHeader>
          <CardContent>
            <div class="bg-muted p-4 rounded-lg text-sm font-mono" role="code" aria-label="Auto-generated table of contents implementation code">
              {`const sections = generateSectionsFromHeadings();

<TableOfContents
  sections={sections}
  position="fixed-right"
  showProgress={true}
  maxLevel={3}
/>`}
            </div>
          </CardContent>
        </Card>

        <!-- Complete Setup -->
        <Card role="listitem">
          <CardHeader>
            <CardTitle className="flex items-center space-x-2">
              <span aria-hidden="true">🚀</span>
              <span>Complete Setup</span>
            </CardTitle>
            <CardDescription>
              Full scroll indicators configuration
            </CardDescription>
          </CardHeader>
          <CardContent>
            <div class="bg-muted p-4 rounded-lg text-sm font-mono" role="code" aria-label="Complete scroll indicators setup with multiple components">
              {`// Multiple indicators working together
<ReadingProgress position="top" />
<ScrollProgressRing position="bottom-right" />
<TableOfContents sections={sections} />
<SectionNavigator sections={sections} />
<ScrollToTop showAt={300} variant="fab" />`}
            </div>
          </CardContent>
        </Card>

      </div>
    </div>
  </section>

  <!-- Business Use Cases -->
  <section id="use-cases" class="py-16" role="region" aria-labelledby="use-cases-title">
    <div class="container mx-auto px-4">
      <h2 id="use-cases-title" class="text-3xl font-bold text-foreground text-center mb-12">
        Business Use Cases
      </h2>
      
      <div class="max-w-4xl mx-auto">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8" role="list" aria-label="Business application use cases">
          
          <!-- Content Publishing -->
          <div class="space-y-4" role="listitem">
            <h3 class="text-xl font-semibold flex items-center space-x-2">
              <span aria-hidden="true">📰</span>
              <span>Content Publishing</span>
            </h3>
            <ul class="space-y-2 text-muted-foreground" role="list" aria-label="Content publishing use cases">
              <li role="listitem">• <strong>Blogs & Articles:</strong> Reading progress for engagement</li>
              <li role="listitem">• <strong>News Sites:</strong> Section navigation for long articles</li>
              <li role="listitem">• <strong>Magazines:</strong> Visual progress for reader satisfaction</li>
              <li role="listitem">• <strong>Educational Content:</strong> Learning progress tracking</li>
            </ul>
          </div>

          <!-- Documentation -->
          <div class="space-y-4" role="listitem">
            <h3 class="text-xl font-semibold flex items-center space-x-2">
              <span aria-hidden="true">📚</span>
              <span>Documentation</span>
            </h3>
            <ul class="space-y-2 text-muted-foreground" role="list" aria-label="Documentation use cases">
              <li role="listitem">• <strong>Technical Docs:</strong> TOC with nested navigation</li>
              <li role="listitem">• <strong>API Documentation:</strong> Section-based progress</li>
              <li role="listitem">• <strong>User Guides:</strong> Step-by-step progress tracking</li>
              <li role="listitem">• <strong>Knowledge Base:</strong> Quick section jumping</li>
            </ul>
          </div>

          <!-- E-commerce -->
          <div class="space-y-4" role="listitem">
            <h3 class="text-xl font-semibold flex items-center space-x-2">
              <span aria-hidden="true">🛒</span>
              <span>E-commerce</span>
            </h3>
            <ul class="space-y-2 text-muted-foreground" role="list" aria-label="E-commerce use cases">
              <li role="listitem">• <strong>Product Pages:</strong> Feature section navigation</li>
              <li role="listitem">• <strong>Reviews:</strong> Progress through long review lists</li>
              <li role="listitem">• <strong>Catalogs:</strong> Category navigation indicators</li>
              <li role="listitem">• <strong>Checkout:</strong> Multi-step progress visualization</li>
            </ul>
          </div>

          <!-- Corporate -->
          <div class="space-y-4" role="listitem">
            <h3 class="text-xl font-semibold flex items-center space-x-2">
              <span aria-hidden="true">🏢</span>
              <span>Corporate</span>
            </h3>
            <ul class="space-y-2 text-muted-foreground" role="list" aria-label="Corporate use cases">
              <li role="listitem">• <strong>Landing Pages:</strong> Section dot navigation</li>
              <li role="listitem">• <strong>Annual Reports:</strong> Reading progress tracking</li>
              <li role="listitem">• <strong>Case Studies:</strong> Progress through findings</li>
              <li role="listitem">• <strong>Presentations:</strong> Slide progress indicators</li>
            </ul>
          </div>

        </div>
      </div>
    </div>
  </section>

  <!-- Technical Features -->
  <section id="features" class="py-16 bg-muted/30" role="region" aria-labelledby="features-title">
    <div class="container mx-auto px-4">
      <h2 id="features-title" class="text-3xl font-bold text-foreground text-center mb-12">
        Technical Features
      </h2>
      
      <div class="max-w-4xl mx-auto">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8" role="list" aria-label="Technical features and capabilities">
          
          <!-- Performance -->
          <div class="space-y-4" role="listitem">
            <h3 class="text-xl font-semibold flex items-center space-x-2">
              <span aria-hidden="true">⚡</span>
              <span>Performance Optimized</span>
            </h3>
            <ul class="space-y-2 text-muted-foreground" role="list" aria-label="Performance optimization features">
              <li role="listitem">• <strong>RequestAnimationFrame:</strong> Smooth 60fps animations</li>
              <li role="listitem">• <strong>Passive Listeners:</strong> Non-blocking scroll events</li>
              <li role="listitem">• <strong>Throttled Updates:</strong> Efficient state management</li>
              <li role="listitem">• <strong>GPU Acceleration:</strong> CSS transforms for smoothness</li>
            </ul>
          </div>

          <!-- Accessibility -->
          <div class="space-y-4" role="listitem">
            <h3 class="text-xl font-semibold flex items-center space-x-2">
              <span aria-hidden="true">♿</span>
              <span>Accessibility First</span>
            </h3>
            <ul class="space-y-2 text-muted-foreground" role="list" aria-label="Accessibility features">
              <li role="listitem">• <strong>ARIA Labels:</strong> Screen reader friendly navigation</li>
              <li role="listitem">• <strong>Keyboard Support:</strong> Tab navigation and Enter activation</li>
              <li role="listitem">• <strong>Focus Management:</strong> Clear visual focus indicators</li>
              <li role="listitem">• <strong>Semantic HTML:</strong> Proper navigation landmarks</li>
            </ul>
          </div>

          <!-- Customization -->
          <div class="space-y-4" role="listitem">
            <h3 class="text-xl font-semibold flex items-center space-x-2">
              <span aria-hidden="true">🎨</span>
              <span>Highly Customizable</span>
            </h3>
            <ul class="space-y-2 text-muted-foreground" role="list" aria-label="Customization options">
              <li role="listitem">• <strong>Positioning:</strong> Multiple fixed position options</li>
              <li role="listitem">• <strong>Styling:</strong> Colors, sizes, thickness variants</li>
              <li role="listitem">• <strong>Behavior:</strong> Show/hide thresholds, smooth scroll</li>
              <li role="listitem">• <strong>Content:</strong> Auto-generation from existing headings</li>
            </ul>
          </div>

          <!-- Integration -->
          <div class="space-y-4" role="listitem">
            <h3 class="text-xl font-semibold flex items-center space-x-2">
              <span aria-hidden="true">🔧</span>
              <span>Easy Integration</span>
            </h3>
            <ul class="space-y-2 text-muted-foreground" role="list" aria-label="Integration capabilities">
              <li role="listitem">• <strong>React Hook:</strong> useScrollProgress for custom components</li>
              <li role="listitem">• <strong>Auto-detection:</strong> Generate sections from headings</li>
              <li role="listitem">• <strong>TypeScript:</strong> Full type safety and IntelliSense</li>
              <li role="listitem">• <strong>Zero Dependencies:</strong> Built with native APIs</li>
            </ul>
          </div>

        </div>
      </div>
    </div>
  </section>

  <!-- Long Content for Testing -->
  <section id="testing" class="py-16" role="region" aria-labelledby="testing-title">
    <div class="container mx-auto px-4">
      <h2 id="testing-title" class="text-3xl font-bold text-foreground text-center mb-12">
        Scroll Testing Content
      </h2>
      
      <div class="max-w-3xl mx-auto space-y-8">
        
        <h3 id="test-section-1" class="text-2xl font-semibold text-foreground">Test Section 1: Introduction</h3>
        <div role="list" aria-label="Introduction test content sections">
          {Array.from({ length: 3 }, (_, i) => (
            <Card key={`intro-${i}`} role="listitem">
              <CardHeader>
                <CardTitle>Introduction Part {i + 1}</CardTitle>
                <CardDescription>
                  Testing scroll behavior with content sections
                </CardDescription>
              </CardHeader>
              <CardContent>
                <p class="text-muted-foreground leading-relaxed">
                  Lorem ipsum dolor sit amet, consectetur adipiscing elit. 
                  Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. 
                  Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris 
                  nisi ut aliquip ex ea commodo consequat. Notice how the scroll indicators 
                  respond as you move through this content.
                </p>
              </CardContent>
            </Card>
          ))}
        </div>

        <h3 id="test-section-2" class="text-2xl font-semibold text-foreground">Test Section 2: Features</h3>
        <div role="list" aria-label="Features test content sections">
          {Array.from({ length: 4 }, (_, i) => (
            <Card key={`features-${i}`} role="listitem">
              <CardHeader>
                <CardTitle>Feature {i + 1}</CardTitle>
                <CardDescription>
                  Demonstrating scroll tracking across sections
                </CardDescription>
              </CardHeader>
              <CardContent>
                <p class="text-muted-foreground leading-relaxed">
                  The table of contents should highlight this section as you scroll through it. 
                  The reading progress bars show your overall progress through the entire page, 
                  while the section navigator indicates which major section you're currently viewing.
                </p>
                <p class="text-muted-foreground leading-relaxed mt-4">
                  All indicators are synchronized and update smoothly as you scroll, 
                  providing an excellent user experience for navigation and progress tracking.
                </p>
              </CardContent>
            </Card>
          ))}
        </div>

        <h3 id="test-section-3" class="text-2xl font-semibold text-foreground">Test Section 3: Benefits</h3>
        <div role="list" aria-label="Benefits test content sections">
          {Array.from({ length: 3 }, (_, i) => (
            <Card key={`benefits-${i}`} role="listitem">
              <CardHeader>
                <CardTitle>Benefit {i + 1}</CardTitle>
                <CardDescription>
                  Understanding the value of scroll indicators
                </CardDescription>
              </CardHeader>
              <CardContent>
                <p class="text-muted-foreground leading-relaxed">
                  Scroll indicators provide significant value for user engagement and navigation. 
                  They help users understand their progress through content and provide quick 
                  navigation options for long-form content.
                </p>
              </CardContent>
            </Card>
          ))}
        </div>

        <h3 id="test-section-4" class="text-2xl font-semibold text-foreground">Test Section 4: Conclusion</h3>
        <div role="list" aria-label="Conclusion test content sections">
          {Array.from({ length: 2 }, (_, i) => (
            <Card key={`conclusion-${i}`} role="listitem">
              <CardHeader>
                <CardTitle>Conclusion Part {i + 1}</CardTitle>
                <CardDescription>
                  Final thoughts on scroll indicator implementation
                </CardDescription>
              </CardHeader>
              <CardContent>
                <p class="text-muted-foreground leading-relaxed">
                  The scroll indicator system provides a comprehensive solution for modern 
                  web applications. With performance optimization, accessibility features, 
                  and extensive customization options, it's ready for professional use.
                </p>
              </CardContent>
            </Card>
          ))}
        </div>

      </div>
    </div>
  </section>

  <!-- Scroll Progress Ring (Fixed Bottom Right) -->
  <ScrollProgressRing 
    client:load
    size={60}
    position="bottom-right"
    showPercentage={true}
    color="hsl(var(--primary))"
    aria-label="Circular scroll progress indicator showing reading completion percentage in bottom-right corner"
  />

  <!-- Table of Contents (Fixed Right) -->
  <TableOfContents 
    client:load
    sections={[
      { id: 'introduction', title: 'Introduction', level: 1 },
      { id: 'overview', title: 'Overview', level: 1 },
      { id: 'components', title: 'Components', level: 1 },
      { id: 'implementation', title: 'Implementation', level: 1 },
      { id: 'use-cases', title: 'Use Cases', level: 1 },
      { id: 'features', title: 'Features', level: 1 },
      { id: 'testing', title: 'Testing', level: 1 },
      { id: 'test-section-1', title: 'Test Section 1', level: 2 },
      { id: 'test-section-2', title: 'Test Section 2', level: 2 },
      { id: 'test-section-3', title: 'Test Section 3', level: 2 },
      { id: 'test-section-4', title: 'Test Section 4', level: 2 }
    ]}
    position="fixed-right"
    showProgress={true}
    maxLevel={2}
    aria-label="Table of contents navigation with automatic section tracking and smooth scroll"
  />

  <!-- Section Navigator (Fixed Left) -->
  <SectionNavigator 
    client:load
    sections={[
      { id: 'introduction', title: 'Introduction' },
      { id: 'components', title: 'Components' },
      { id: 'implementation', title: 'Implementation' },
      { id: 'use-cases', title: 'Use Cases' },
      { id: 'features', title: 'Features' },
      { id: 'testing', title: 'Testing' }
    ]}
    position="left"
    showLabels={true}
    aria-label="Section navigator with dot indicators and section labels for quick navigation"
  />

  <!-- Scroll to Top Button -->
  <ScrollToTop 
    client:load
    showAt={400}
    position="bottom-left"
    variant="fab"
    showProgress={false}
    aria-label="Scroll to top button with smooth animation, appears after scrolling down 400 pixels"
  />

</Layout>