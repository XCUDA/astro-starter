---
// SectionTemplates.astro - Pre-configured Business Section Templates (Final Fixed)
// Ready-to-use sections for rapid page construction
// Path: src/components/layouts/SectionTemplates.astro

import GridContainer from './GridContainer.astro';
import { Badge } from '../ui/badge';
import { Button } from '../ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '../ui/card';

export interface Props {
  template: 'hero' | 'features' | 'testimonials' | 'pricing' | 'team' | 'stats' | 'cta' | 'process' | 'faq';
  variant?: 'default' | 'minimal' | 'business' | 'startup';
  class?: string;
  title?: string;
  subtitle?: string;
  description?: string;
  showBackground?: boolean;
}

const {
  template,
  variant = 'default',
  class: className = '',
  title,
  subtitle, 
  description,
  showBackground = false
} = Astro.props;

// Background classes for sections
const backgroundClass = showBackground 
  ? 'bg-gradient-to-br from-primary/5 via-background to-secondary/5' 
  : 'bg-background';

const sectionClass = `py-16 md:py-24 ${backgroundClass} ${className}`.trim();
---

<section class={sectionClass}>
  
  {template === 'hero' && (
    <GridContainer variant="wide" columns={2} alignItems="center" gap="xl">
      <!-- Hero Content -->
      <div class="space-y-6">
        {subtitle && (
          <Badge variant="secondary" className="text-sm font-medium">
            {subtitle}
          </Badge>
        )}
        
        <h1 class="text-4xl lg:text-6xl font-bold text-foreground leading-tight">
          {title || "Build Amazing Websites with Our Professional Starter"}
        </h1>
        
        <p class="text-xl text-muted-foreground leading-relaxed max-w-2xl">
          {description || "A modern, production-ready Astro 5 starter with TailwindCSS 4, shadcn/ui, and TypeScript. Optimized for performance, accessibility, and professional development workflows."}
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4">
          <Button size="lg" className="text-lg px-8">
            Get Started
          </Button>
          <Button variant="outline" size="lg" className="text-lg px-8">
            View Components
          </Button>
        </div>
      </div>
      
      <!-- Hero Visual -->
      <div class="flex items-center justify-center">
        <div class="w-full max-w-lg aspect-square bg-gradient-to-br from-primary/20 to-secondary/20 rounded-2xl border border-border flex items-center justify-center">
          <div class="text-6xl">🚀</div>
        </div>
      </div>
    </GridContainer>
  )}

  {template === 'features' && (
    <GridContainer variant="default" columns={1} gap="lg">
      <!-- Section Header -->
      <div class="text-center mb-16">
        {subtitle && (
          <Badge variant="secondary" className="text-sm font-medium mb-4">
            {subtitle}
          </Badge>
        )}
        <h2 class="text-3xl lg:text-4xl font-bold text-foreground mb-4">
          {title || "Everything You Need"}
        </h2>
        <p class="text-lg text-muted-foreground max-w-3xl mx-auto">
          {description || "Comprehensive toolset for building modern, professional websites with best practices built-in."}
        </p>
      </div>
      
      <!-- Features Grid -->
      <GridContainer columns={3} gap="lg">
        <Card className="hover:shadow-lg transition-all duration-200 hover:scale-105">
          <CardHeader className="text-center">
            <div class="text-3xl mb-2">⚡</div>
            <CardTitle>Lightning Fast</CardTitle>
            <CardDescription>
              Astro 5 with Islands architecture for optimal performance
            </CardDescription>
          </CardHeader>
          <CardContent>
            <ul class="text-sm space-y-2 text-muted-foreground">
              <li>• Static site generation</li>
              <li>• Selective hydration</li>
              <li>• Core Web Vitals optimized</li>
            </ul>
          </CardContent>
        </Card>
        
        <Card className="hover:shadow-lg transition-all duration-200 hover:scale-105">
          <CardHeader className="text-center">
            <div class="text-3xl mb-2">🎨</div>
            <CardTitle>Beautiful Design</CardTitle>
            <CardDescription>
              TailwindCSS 4 and shadcn/ui for modern aesthetics
            </CardDescription>
          </CardHeader>
          <CardContent>
            <ul class="text-sm space-y-2 text-muted-foreground">
              <li>• Dark mode support</li>
              <li>• Responsive layouts</li>
              <li>• Accessible components</li>
            </ul>
          </CardContent>
        </Card>
        
        <Card className="hover:shadow-lg transition-all duration-200 hover:scale-105">
          <CardHeader className="text-center">
            <div class="text-3xl mb-2">🔧</div>
            <CardTitle>Developer Ready</CardTitle>
            <CardDescription>
              TypeScript, professional tooling, and best practices
            </CardDescription>
          </CardHeader>
          <CardContent>
            <ul class="text-sm space-y-2 text-muted-foreground">
              <li>• Full type safety</li>
              <li>• Modern development</li>
              <li>• Production optimized</li>
            </ul>
          </CardContent>
        </Card>
      </GridContainer>
    </GridContainer>
  )}

  {template === 'stats' && (
    <GridContainer variant="default" columns={4} gap="lg" alignItems="center">
      <div class="text-center">
        <div class="text-4xl font-bold text-primary mb-2">100+</div>
        <div class="text-sm text-muted-foreground">Components</div>
      </div>
      <div class="text-center">
        <div class="text-4xl font-bold text-primary mb-2">26+</div>
        <div class="text-sm text-muted-foreground">UI Elements</div>
      </div>
      <div class="text-center">
        <div class="text-4xl font-bold text-primary mb-2">100</div>
        <div class="text-sm text-muted-foreground">Lighthouse Score</div>
      </div>
      <div class="text-center">
        <div class="text-4xl font-bold text-primary mb-2">5+</div>
        <div class="text-sm text-muted-foreground">Layout Variants</div>
      </div>
    </GridContainer>
  )}

  {template === 'pricing' && (
    <GridContainer variant="default" columns={1} gap="lg">
      <!-- Section Header -->
      <div class="text-center mb-16">
        <h2 class="text-3xl lg:text-4xl font-bold text-foreground mb-4">
          {title || "Simple, Transparent Pricing"}
        </h2>
        <p class="text-lg text-muted-foreground max-w-3xl mx-auto">
          {description || "Choose the perfect plan for your project needs. All plans include full source code and lifetime updates."}
        </p>
      </div>
      
      <!-- Pricing Cards -->
      <GridContainer columns={3} gap="lg">
        <Card className="relative">
          <CardHeader className="text-center">
            <CardTitle>Starter</CardTitle>
            <CardDescription>Perfect for small projects</CardDescription>
            <div class="text-3xl font-bold text-primary">Free</div>
          </CardHeader>
          <CardContent>
            <ul class="space-y-3 text-sm">
              <li class="flex items-center space-x-2">
                <span class="text-green-500">✓</span>
                <span>Basic components</span>
              </li>
              <li class="flex items-center space-x-2">
                <span class="text-green-500">✓</span>
                <span>Community support</span>
              </li>
              <li class="flex items-center space-x-2">
                <span class="text-green-500">✓</span>
                <span>MIT License</span>
              </li>
            </ul>
            <Button className="w-full mt-6" variant="outline">
              Get Started
            </Button>
          </CardContent>
        </Card>
        
        <Card className="relative border-primary shadow-lg scale-105">
          <div class="absolute -top-3 left-1/2 transform -translate-x-1/2">
            <Badge className="bg-primary text-primary-foreground">Most Popular</Badge>
          </div>
          <CardHeader className="text-center">
            <CardTitle>Professional</CardTitle>
            <CardDescription>For serious projects</CardDescription>
            <div class="text-3xl font-bold text-primary">$99</div>
          </CardHeader>
          <CardContent>
            <ul class="space-y-3 text-sm">
              <li class="flex items-center space-x-2">
                <span class="text-green-500">✓</span>
                <span>All components</span>
              </li>
              <li class="flex items-center space-x-2">
                <span class="text-green-500">✓</span>
                <span>Priority support</span>
              </li>
              <li class="flex items-center space-x-2">
                <span class="text-green-500">✓</span>
                <span>Commercial license</span>
              </li>
              <li class="flex items-center space-x-2">
                <span class="text-green-500">✓</span>
                <span>Lifetime updates</span>
              </li>
            </ul>
            <Button className="w-full mt-6">
              Get Started
            </Button>
          </CardContent>
        </Card>
        
        <Card className="relative">
          <CardHeader className="text-center">
            <CardTitle>Enterprise</CardTitle>
            <CardDescription>For large organizations</CardDescription>
            <div class="text-3xl font-bold text-primary">Custom</div>
          </CardHeader>
          <CardContent>
            <ul class="space-y-3 text-sm">
              <li class="flex items-center space-x-2">
                <span class="text-green-500">✓</span>
                <span>Everything included</span>
              </li>
              <li class="flex items-center space-x-2">
                <span class="text-green-500">✓</span>
                <span>Custom development</span>
              </li>
              <li class="flex items-center space-x-2">
                <span class="text-green-500">✓</span>
                <span>Dedicated support</span>
              </li>
              <li class="flex items-center space-x-2">
                <span class="text-green-500">✓</span>
                <span>Training included</span>
              </li>
            </ul>
            <Button className="w-full mt-6" variant="outline">
              Contact Sales
            </Button>
          </CardContent>
        </Card>
      </GridContainer>
    </GridContainer>
  )}

  {template === 'cta' && (
    <GridContainer variant="default" columns={1} gap="lg">
      <div class="text-center max-w-4xl mx-auto">
        <h2 class="text-3xl lg:text-4xl font-bold text-foreground mb-6">
          {title || "Ready to Get Started?"}
        </h2>
        <p class="text-xl text-muted-foreground mb-8 leading-relaxed">
          {description || "Join thousands of developers building amazing websites with our professional starter. Start your project today and see the difference quality makes."}
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <Button size="lg" className="text-lg px-8">
            Start Building Now
          </Button>
          <Button variant="outline" size="lg" className="text-lg px-8">
            View Documentation
          </Button>
        </div>
      </div>
    </GridContainer>
  )}

  {template === 'team' && (
    <GridContainer variant="default" columns={1} gap="lg">
      <!-- Section Header -->
      <div class="text-center mb-16">
        <h2 class="text-3xl lg:text-4xl font-bold text-foreground mb-4">
          {title || "Meet Our Team"}
        </h2>
        <p class="text-lg text-muted-foreground max-w-3xl mx-auto">
          {description || "The passionate developers and designers behind this amazing starter template."}
        </p>
      </div>
      
      <!-- Team Grid -->
      <GridContainer columns={3} gap="lg">
        <Card className="text-center hover:shadow-lg transition-all duration-200">
          <CardContent className="pt-6">
            <div class="w-24 h-24 bg-gradient-to-br from-primary/20 to-secondary/20 rounded-full mx-auto mb-4 flex items-center justify-center text-2xl">
              👨‍💻
            </div>
            <h3 class="font-semibold text-lg mb-2">John Developer</h3>
            <p class="text-muted-foreground text-sm mb-4">Lead Frontend Developer</p>
            <p class="text-xs text-muted-foreground">Passionate about creating amazing user experiences with modern web technologies.</p>
          </CardContent>
        </Card>
        
        <Card className="text-center hover:shadow-lg transition-all duration-200">
          <CardContent className="pt-6">
            <div class="w-24 h-24 bg-gradient-to-br from-primary/20 to-secondary/20 rounded-full mx-auto mb-4 flex items-center justify-center text-2xl">
              👩‍🎨
            </div>
            <h3 class="font-semibold text-lg mb-2">Jane Designer</h3>
            <p class="text-muted-foreground text-sm mb-4">UI/UX Designer</p>
            <p class="text-xs text-muted-foreground">Focused on creating beautiful, accessible designs that users love to interact with.</p>
          </CardContent>
        </Card>
        
        <Card className="text-center hover:shadow-lg transition-all duration-200">
          <CardContent className="pt-6">
            <div class="w-24 h-24 bg-gradient-to-br from-primary/20 to-secondary/20 rounded-full mx-auto mb-4 flex items-center justify-center text-2xl">
              👨‍💼
            </div>
            <h3 class="font-semibold text-lg mb-2">Mike Manager</h3>
            <p class="text-muted-foreground text-sm mb-4">Project Manager</p>
            <p class="text-xs text-muted-foreground">Ensuring projects run smoothly and deliver exceptional value to our clients.</p>
          </CardContent>
        </Card>
      </GridContainer>
    </GridContainer>
  )}

  {template === 'process' && (
    <GridContainer variant="default" columns={1} gap="lg">
      <!-- Section Header -->
      <div class="text-center mb-16">
        <h2 class="text-3xl lg:text-4xl font-bold text-foreground mb-4">
          {title || "How It Works"}
        </h2>
        <p class="text-lg text-muted-foreground max-w-3xl mx-auto">
          {description || "Get up and running in just a few simple steps. Our streamlined process makes it easy to start building amazing websites."}
        </p>
      </div>
      
      <!-- Process Steps -->
      <GridContainer columns={4} gap="lg">
        <div class="text-center relative">
          <div class="w-16 h-16 bg-primary text-primary-foreground rounded-full mx-auto mb-4 flex items-center justify-center text-xl font-bold">
            1
          </div>
          <h3 class="font-semibold text-lg mb-2">Download</h3>
          <p class="text-sm text-muted-foreground">Get the starter template and install dependencies with a single command.</p>
        </div>
        
        <div class="text-center relative">
          <div class="w-16 h-16 bg-primary text-primary-foreground rounded-full mx-auto mb-4 flex items-center justify-center text-xl font-bold">
            2
          </div>
          <h3 class="font-semibold text-lg mb-2">Customize</h3>
          <p class="text-sm text-muted-foreground">Adapt the design system and components to match your brand and requirements.</p>
        </div>
        
        <div class="text-center relative">
          <div class="w-16 h-16 bg-primary text-primary-foreground rounded-full mx-auto mb-4 flex items-center justify-center text-xl font-bold">
            3
          </div>
          <h3 class="font-semibold text-lg mb-2">Build</h3>
          <p class="text-sm text-muted-foreground">Create your pages using our pre-built components and layout system.</p>
        </div>
        
        <div class="text-center relative">
          <div class="w-16 h-16 bg-primary text-primary-foreground rounded-full mx-auto mb-4 flex items-center justify-center text-xl font-bold">
            4
          </div>
          <h3 class="font-semibold text-lg mb-2">Deploy</h3>
          <p class="text-sm text-muted-foreground">Launch your site with optimized performance and excellent SEO out of the box.</p>
        </div>
      </GridContainer>
    </GridContainer>
  )}

</section>